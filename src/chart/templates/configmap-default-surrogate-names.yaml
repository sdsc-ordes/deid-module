{{ $fullname := include "surrogate.fullname" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ $fullname }}-surrogate-names"
  namespace: {{ $.Values.namespace }}
data:
  surrogate_names.csv: |-
{{ .Files.Get "conf/surrogate/surrogate_db/surrogate_names.csv" | indent 4 }}
