{{ $fullname := include "surrogate.fullname" . }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ $fullname }}-surrogate-names"
  namespace: {{ $.Values.namespace }}
data:
  example_names.csv: |-
{{ .Files.Get "conf/surrogate/example_names.csv" | indent 4 }}
